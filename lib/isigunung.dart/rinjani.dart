import 'package:flutter/material.dart';

class RinjaniPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gunung Rinjani'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Gunung Rinjani'),
      ),
    );
  }
}
