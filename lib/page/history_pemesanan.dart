import 'package:flutter/material.dart';

class HistoryPemesanan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("History Pemesanan"),
      ),
      body: Center(
        child: const Text("This is the History Pemesanan Page"),
      ),
    );
  }
}
