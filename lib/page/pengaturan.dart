import 'package:flutter/material.dart';

class Pengaturan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Pengaturan"),
      ),
      body: Center(
        child: const Text("This is the Pengaturan Page"),
      ),
    );
  }
}
